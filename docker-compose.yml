services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"

  generator:
    build:
      context: ./DataGeneration/
    container_name: generator
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis
